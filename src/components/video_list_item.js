import React from "react";

const VideoListItem = ({ video }) => {
  // const video = props.video;
  // This same as replacing {video} instead of props
  // console.log(video);
  return <li>Video</li>;
};

export default VideoListItem;
